# secrets - never commit
.env
.claude/settings.local.json

# python
.venv/
venv/
__pycache__/
*.pyc

# The database, fetched page text, and work queue ARE tracked (not the usual practice for a
# sqlite file, but there is no other persistent storage between a GitHub Actions run that
# discovers/fetches leads and the Claude Code session that reads them - GitHub itself is the
# only shared disk both sides have). Only Claude's per-run scratch file stays untracked.
data/work/claude_results.jsonl

# os / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
